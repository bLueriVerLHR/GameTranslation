# KiriKiri → TyranoScript 手机构建进度记录（历史归档）

> ⚠️ **历史归档，不要照它执行。** 这里记录的是「KiriKiri 手机转换」那一轮
> 会话的完成轨迹：每一轮试玩反馈、定位到的缺陷与修复方式。当前的做法、
> 能力与验收层次看 `docs/kirikiri-tyrano.md`（尤其 §3 常见缺口、§5 已推翻
> 的判断、§7 验收层次与降级政策）。
>
> 保留价值在于**修复轨迹与失败模式**：某条已修条目背后的定位方法（差异
> 复现 → 合成数据回归 → 工作副本验证）仍然适用于新的转换问题。
>
> 下面的条目都是**当时的**状态，「待用户试玩确认」之类在归档日可能已过期；
> 具体勾选状态不要当成现状。

## 当时的进度记录

- [x] 收集六项主要体验问题。
- [x] 审查现有转换器与兼容层，找到可复现代码缺陷。
- [x] 更新 README 与 AGENTS 中过时的转换目标。
- [x] 建立可重复生成的桌面浏览器测试构建与复现入口。
- [x] 随机抽样转换性能并记录全量预估。
- [x] 完成对话、名字和选择的首轮代码修复与浏览器回归。
- [x] 完成调试快进与语音状态可靠性修复。
- [x] 定位并修复标题菜单黑屏：增量转换的资产索引里 `.MA` 地图描述文件抢占了
      同名画面（菜单不可见但点击区域生效，误触不可见的退出项会关掉窗口）。
- [x] 处理浏览器/JoiPlay 下 `[close]` 直接关窗的行为：改走 Tyrano 的返回标题流程。
- [x] 加入第一版手机触摸工具栏，并在浏览器验证展开、收起与点击隔离。
- [x] 完成第二轮阅读界面：用源脚本几何区分正文框、名字框和桌面辅助框；
      正文使用半透明深色衬底与阴影，名字保留在正文框左上方，隐藏已无功能的
      桌面按钮占位和 Tyrano 默认圆形菜单按钮。
- [x] 完成选择界面回归：提示与各选项集中在独立深色面板，每项保持独立触摸区；
      合成三选项场景已在桌面浏览器确认文字不溢出、不重复，点击后旧选项清除并
      进入对应分支。真实场景仍需继续随机抽样，Android 结果另行记录。
- [x] 将名字栏和正文衬底统一为同宽的半透明深色区域，名字按正文左边距对齐；
      不再保留独立的小方框外观。
- [x] 修复快进后隐藏消息的恢复流程：隐藏会先退出快进并走引擎原生生命周期；
      浏览器实测恢复后点击和回车均可继续推进。
- [x] 修复存档与历史页面叠加：系统页面串行占用菜单层，打开期间隐藏手机工具栏；
      浏览器实测往返两个页面没有重叠。
- [x] 压缩选项纵向间距并区分提示、空换行和链接；受控三选项场景实测选择后
      相关节点全部清除，后续对白不再进入旧选项面板。
- [x] 恢复高优先级补丁脚本覆盖：将所有档案按优先级叠加到同一转换输入后重建，
      浏览器实测正文使用覆盖后的目标语言版本。
- [x] 完成标题关键路径实测：标题进入鉴赏、鉴赏返回标题、标题进入正篇并推进到
      目标语言对白均成功；区域图的实际点击坐标按缩放后的游戏窗口换算。
- [x] 修复手机菜单 SKIP：改走 Tyrano 原生 `skipstart` 生命周期，处于等待点时会先
      推进再持续快进；合成四句场景中浏览器实测按钮状态与连续推进均正常。
- [x] 加固选择结束清理：在链接点击的捕获阶段标记旧选择层，并在下一次正文渲染前
      创建新段落；浏览器实测选择后的正文回到底部消息框，不再进入中央选择面板。
- [x] 恢复鉴赏首页和大图层场景：按脚本动态扩展数字图层，取消 Tyrano 条件搜索的
      固定距离上限；浏览器实测鉴赏首页、缩略图和三类入口正常显示。
- [x] 修复鉴赏回放结束后不回图库（场景结束后直接接上剧情）：宏体里的 `[return]`
      在 Tyrano 的运行期宏帧栈下会跳回宏的调用点，兼容层改为在 `[call]` 时记录宏栈
      深度、`[return]` 时先截断再交回引擎（恢复 KAG3 内联宏语义）。实测轨迹从
      `return(define.ks:2964)` 正确走到 `jump seen.ks(seen_set.ks:257)`，场景标志清除、
      栈归零，落在鉴赏首页（该页渲染已验证）。**待用户从标题真实路径试玩确认**。
- [x] 修复人物语音/语音重播/自动播放全部哑掉：`[iscript]` 顶层函数没被导出为全局
      （函数声明的花括号换行未匹配转换器正则），全局查找静默落到垫片兜底桩。
- [x] 鉴赏回放体验二轮（用户试玩反馈，已修并实测）：① 影片静音 —— Tyrano 的
      `layermode_movie` 不传 `volume` 时 `video.volume = 0`，KAG3 的 `[playvideo]`
      又没有该参数，于是动画鉴赏只有 BGM 在响；shim 现默认补 `volume=100`
      （实测 `volume: 0 → 1`、未静音、未暂停）。② 影片只占画布一部分 —— 新增
      `--video-fit fill`（等比铺满画布；本作 4:3 片源配 4:3 画布无色边，实测元素尺寸
      等于画布）。③ 回放开场曲与图库同曲时无缝衔接、听不出“回放开始了” —— 垫片在
      `tf.now_pv==1` 且 storage 同名（按 stem 比）时清 `stat.current_bgm` 使其走完整
      播放路径（实测 Howler 实例 1→2），`target=se` 已排除；注：11 个回放里有 6 个
      开场曲本来就是菜单曲，那是原作脚本选择。④ 回放中途无法退出（游戏自己在回放
      开始时用 `[rclick enabled=true jump=false]` 解除右键返回，手机又无右键）——
      在手机控制面板（`#kag3-mobile-panel`）里新增 `GALLERY` 条目（仅
      `tf.now_pv==1` 且有 `*return*` 返回标签时出现），点击清空 call/macro/if 帧后
      跳到游戏自己的 `*return_seen` 并复位 `tf.now_pv`/`sf.seenflg`（不复位会让
      条目不隐、图库存读档按钮被 SYSMENU 分支隐掉）。**做成浮动按钮会与菜单键
      （☰）重叠**（owner 看图发现），故必须放在面板行内。实测：点击后 `seen.ks`、
      栈归零、条目自动隐藏。
- [x] 消息底衬实验（用户要求）：新增 `--msg-style bare` —— 去掉主消息层
      （`message0`）的底衬图，改给文字加描边 + 阴影、不透明度 0.8；名字框在
      `message1` 层不受影响。默认仍为 `plate`，待用户看效果决定保留与否。
- [ ] 复核：用 JS 直接跳进鉴赏回放内部标签（绕过图库自身初始化）时，回库后的
      鉴赏画面会出现空图层黑屏；真实入口路径未复现，需在用户试玩时确认是否存在
      真实回库渲染问题，不要先当成缺陷。
      **已核实（2026-09-15）**：真实入口路径（缩略图 → 确认 YES → 回放 → 结束）
      回库后终态为 `seen.ks`、`sf.seenflg=0`、栈归零，base 图层已重新加载
      `fgimage/seen.png` 且可见；之前的“黑屏”是浏览器自动化会话漂移/离屏会话
      造成的截图假象，**不存在真实回库渲染问题**。仍请 owner 在真实浏览器里看一眼。
- [ ] 快进不越过 `[p]`（改页点击等待）：skip 模式会在停点处停下，需靠 `--fast-skip`
      调试驱动或手动点击才能继续；交付构建（无 `--fast-skip`）对玩家无影响，
      但如果希望“开快进就一直跑”，需在调试/测试构建里带上该选项。
- [x] 为浏览器无法直接执行的旧动画描述加入代表帧回退，避免进入动画鉴赏后只显示
      黑屏；视频型内容继续复用已转换的 WebM 播放路径。
- [ ] 固定 Tyrano 引擎依赖版本，并评估迁移为可拉取的 submodule；运行时修复仍由
      转换器生成，待有公开可访问的引擎 fork 后再把必须修改引擎源码的部分下沉。
- [ ] 由用户在 Android JoiPlay 上试玩第一轮候选构建。
- [ ] 完成手机控制与存读档闭环。
- [ ] 继续核对鉴赏的分页、返回、逐项解锁和场景回放结束状态；旧插件动画当前为代表帧
      降级，待取得可还原的帧序列或时序数据后再恢复完整循环。
- [ ] 完成 Android 实机验收与交付。

当前不承诺整体工期：前三项界面问题需先复现，动画和鉴赏范围需先盘点。
每个阶段完成后记录真实耗时、剩余缺口和下一份可试玩产物。
