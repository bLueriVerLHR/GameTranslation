# 经验库 · Tyrano

> **先读这篇**：本文收录「TyranoScript / TyranoBuilder」相关的实战经验
> （由原单文件经验库拆分而来，原 §7.3.1、§10-KAG3 移植）。
> 完整构建/翻译指南见 `docs/tyrano.md`。其他主题经验见：
> [解密 / 解包 / Repacker 识别](experience-decrypt.md) ·
> [音频 / 清理 / 打包](experience-audio-clean.md) ·
> [翻译](experience-translation.md) ·
> [其他 / 杂项](experience-misc.md) ·
> [经验库索引](experience.md)。

## 1. TyranoScript `[bgmovie]` 自动播放策略卡死

TyranoScript 引擎的 `[bgmovie]` 在 `tyrano/plugins/kag/kag.tag_ext.js`
里直接 `video.play()`，被拒后 `wait_bgmovie` 永久等待 → 标题黑屏冻结
（PC 浏览器和 JoiPlay 都可能复现，取决于该 origin 的播放权限历史）。
修复已固化进 `tyrano/pipeline.py fix-autoplay`（幂等、可逆）：`play()`
promise 被拒时挂一次性 click/touchstart/keydown 监听，首次用户交互后
重播并移除监听。验证方式：把真实交付构建里手工打的补丁反解成原始文件，
用模块重打，`cmp` 与交付构建字节一致。注意 minified 文件里
`video2.play()j_video2.css` 无分号（ASI），替换时保留该特征。

## 2. KAG3 → TyranoScript 移植：停车标签与可点击地图（KiriKiri 引擎移植）

- **KAG3 `[s]` 不是点击等待**：本作（改版 KAG3 系统）的 `s` 处理器是
  `inSleep=true; return -1` = 场景结束，点击不做任何事；流程恢复只靠
  `window.process()/goToLabel`、链接、地图。移植到 Tyrano 时若映射成 `[l]`
  （点击推进），标题/菜单死区点击会误推进、串标签。正确做法：自定义
  `[kag3stop]` = stronglyStop + hideEventLayer（事件层隐藏，点击无效），
  地图/链接/jump 不依赖流程状态照常工作。转换期需确认目标游戏所有 `[s]`
  后跟的是标签/endmacro（抽样全量统计，而不是凭经验）。
- **KAG3 可点击地图的生命周期**：地图挂在图层上，`window.process()`/jump
  **不清地图**；只有「该图层加载新图片」（`loadImages` →
  `clearProvinceActions`）或显式 `[mapdisable]` 才清。挂接方式有两种：
  显式 `[mapaction]`，以及 loadImages 的**自动挂接**（`X.ma` 与图片同名时
  自动加载）。移植时若在跳转时清地图，标题 hover（onenter → process 跳转）
  会把地图清掉 → 之后点击全部失效。按原语义只在该图层换图/显式 disable 时清。
- **`[s]` 等标签映射的正则要保行尾**：`re.sub(r'^\[s\]\s*$', ...)` 的 `\s*$`
  会吞掉 `\r\n`，下一行标签被并到上一行（`[s]` + `*label` → `[kag3stop]*label`，
  标签行变成文本）。改成 `r'^\[s\](\s*)$'` + lambda 保留 group(1)。
- **KAG3 `[ch]` 是正文渲染标签**：名字窗、选择肢文字、菜单项全靠它
  （`[ch text=%n]`、`[ch text=&sentaku[0].txt]`）。移植时转发给目标引擎的
  文本标签（Tyrano 的 text 标签自己 nextOrder，shim 不要再推进）。no-op
  shim 会让名字/选择肢/菜单文字全部空白——这类"看似无害"的 no-op 名单要
  逐个核对用途。
- **右击推进的屏幕**：`[rclick jump=true storage=X target=Y]` 驱动的屏
  （coming soon/lineup 等）在移动端没有右击会卡死。移植时实现 rclick 标签
  + 触摸兜底（无活动地图时左键也触发）。
- **多标签行不能只按标签重建**：正文可能夹在 `[font]`、`[r]`、`[l]` 等
  标签之间。扫描器必须保留每个标签的源区间，只改写区间本身，再把间隙正文
  原样拼回。引号内的 `]` 与转义括号也要纳入测试。
- **`@tag` 与 `[tag]` 必须经过同一转换路径**：先把行首语法规范化，再执行
  资源路径、等待和标签语义改写；提前返回会造成两种写法行为分裂。
- **相邻标签分隔符要在解析前规范化**：部分 KAG3 脚本用 `]\[` 连接标签。
  Tyrano 会把反斜杠当转义并把后一个标签显示成正文，严重时还会留下未闭合的
  条件块。只删除紧跟 `]` 且紧邻 `[` 的反斜杠，不能吞普通正文转义。
- **选择文字应写入当前链接 span**：Tyrano 的链接标签会创建活动 span；
  `[ch]` 若另建平级 span 或段落，显示文本与点击目标会分离，并可能在重复进入
  时留下重叠区域。使用当前消息层、当前段落和当前 span，选择结束后按引擎
  生命周期清理。
- **音频异步回调必须校验播放实例**：复用声道时，旧元素的 `ended`、`error`
  或 `play()` 拒绝可能晚于新声音到达。回调只有在元素仍是声道当前实例时才能
  修改状态；播放前先设状态，并覆盖同步抛错与 Promise 拒绝两条失败路径。
- **增量转换仍要重建资产索引**：`--scenario-only` 会保留输出目录中的图片、
  音频与地图文件。若只从精简源目录生成运行时映射，省略扩展名的引用会退回
  Tyrano 默认文件夹并产生 404。增量模式须把现有 `data/` 资产合并进映射。
- **资产索引合并必须是「图片优先」，不能用 setdefault**：KAG3 的可点击地图
  描述文件与同一屏的画面常共用基础名（`X.MA` 与 `X.png`）。精简源目录里
  往往只剩 `.MA`（图片常经由 `[image storage=%param]` 这类宏参数引用，离线
  收集引用时容易漏掉），先写入的 `.MA` 会占住基础名，后合并进来的图片被
  `setdefault` 丢弃。运行时表现是**画面全黑但点击区域照旧生效**：省略扩展名
  的图片引用把 `.ma` 文本文件塞给 `<img>`，浏览器解码失败且不报错（HTTP 200、
  `js_errors: 0`），而区域图 `X_P.png` 是独立映射，命中测试仍然正确。实测
  后果：菜单不可见却可点，误触不可见的退出项会走到 `[close]`，确认后
  `window.close()` 直接关掉窗口（app 模式/无头窗口真会关，普通标签页会被浏览器
  拦下）。合并规则必须显式表达「图片胜过同名非图片」，两张图片之间仍由源目录
  决定。排查手法：对照运行时映射中 `X` 与 `X.png` 两个键的值（值为 `.ma` 即命中），
  并核对服务器访问日志里该图片是否**从未**被请求。
- **全屏点击捕获必须先排除控制界面**：可点击地图常在 document 捕获阶段处理
  事件；若命中后停止传播，后加的手机工具栏即使层级最高也收不到点击。命中
  测试应把工具栏、按钮和表单控件识别为界面元素，让事件继续到目标控件；工具栏
  自己处理后再停止传播，避免同时推进剧情或触发地图。
- **移除桌面按钮后也要处理空外框**：只丢弃 `[button]` 会留下原作给按钮行准备的
  小消息层，结果是正文旁边仍有一排空底图。可在 `[position]` 执行后按画布比例
  分类：宽而高的是正文框，中等宽度的矮框是名字框，极窄矮框才是桌面辅助框。
  名字框必须设置最小宽度门槛，否则小按钮占位会被误判成名字框重新显示。
- **选择面板要从链接元素反向标记整层**：`[ch]` 同时用于名字和选择文字，不能把
  所有 `[ch]` 都居中。仅当当前 span 属于 Tyrano 的链接事件元素时，才给选项、
  消息内层和所在图层加分类；随后把提示和全部链接排入一个自适应高度面板。源宏
  可能只给第一项设置白色，移动样式需要覆盖选项内部 span 的颜色，否则后续项会
  在深色衬底上显示成黑字。
- **替换了控制面板就应隐藏 Tyrano 默认菜单图标**：默认右下角圆形按钮会覆盖正文
  或选择区。自定义面板已连接真实存读档、历史、自动和快进动作时，可隐藏默认
  `button_menu.png`，并保留一个至少 44 像素的折叠入口。
- **隐藏消息必须走引擎完整生命周期**：只调用 `hideMessageLayers()` 会隐藏画面，
  却不会执行 `hidemessage` 标签恢复事件层所需的状态转换。快进中断后尤其容易
  留下“画面恢复但点击和回车均不能推进”的状态。隐藏前应退出快进，隐藏时调用
  原生 `hidemessage`；恢复时显示消息层、清掉点击中文字状态，并在非强停点重新
  显示事件层。
- **系统菜单必须串行打开并独占界面层**：Tyrano 的存档、读档和历史入口会异步
  填充同一个菜单层。连续点击时若不判断菜单层状态、不先清空旧内容，页面会互相
  叠加。打开前应设置 pending 状态、清空菜单层并隐藏游戏工具栏；菜单关闭后再按
  菜单层可见性恢复工具栏。
- **选择宏会留下空换行 span**：选项之间的 `[r]` 常生成仅含 `<br>` 的 span。
  若把所有非链接 span 都当作提示文字，它们会各自取得块级外边距，造成选项纵向
  间隔异常。应按可见文字区分提示与空隙，隐藏空隙；选择完成后同时删除选项、提示
  和空隙节点，并移除消息层的选择态 class，防止后续对白进入旧面板。
- **选择跳转发生在 document 冒泡清理之前**：链接自身的点击处理会同步开始目标标签，
  新正文可能在冒泡阶段清理旧选择层之前写入。应在捕获阶段只标记“正在离开选择”，
  再由下一次正文标签清除旧节点并创建新段落；延迟删除时只删除点击前保存的节点，
  避免误删目标分支立即生成的新选择。
- **菜单快进要调用原生 `skipstart`**：直接 `setSkip(true)` 只改变标志，不会把停在
  `[l]`/`[p]` 的标签泵向前推进，所以按钮看似开启却没有效果。非打字状态下调用
  `skipstart`，打字过程中才直接开启标志；关闭时仍走 `setSkip(false)`。
- **大型鉴赏脚本会突破 Tyrano 的两个固定假设**：KAG3 可用 `[laycount]` 临时请求上百
  个数字图层，而 Tyrano 默认只创建配置中的少量图层；同时 Tyrano 的条件块搜索最多
  查看 2000 个标签。鉴赏缩略图会因此报图层为空或找不到 `[endif]`。兼容层应按需调用
  引擎的图层创建接口，并把搜索上限改为当前场景剩余标签数。
- **旧动画描述不能当作浏览器图片加载**：有些 KAG3 动画插件用带前缀的描述名引用
  一张同名代表帧，描述数据不会被浏览器图像标签解码。若没有可重建的帧序列与时序，
  应明确降级到已转换的代表帧，避免黑屏；视频型鉴赏仍走媒体转换和视频播放路径。

## 3. 宏体里的 `[return]`：Tyrano 会跳错地方（鉴赏回放结束不回图库）

**症状**：从鉴赏进 CG 回放，场景结束后**不回到图库**，而是直接接上后续剧情
（场景标志未清除、调用栈看似正常，无报错）。

**机制**（已实测定位，引擎源码里也写了）：

- KAG3 的宏是**解析期内联**的，所以宏体里的 `[return]` 等价于「调用方顺序流里的一条
  普通 return 标签」→ 从最内层 `[call]` 返回（“回放结束”正是这个用法：
  `[macro name=PV_END]` → `[if exp="tf.now_pv == 1"][return][endif]`）。
- Tyrano 的宏是**运行期帧栈**（`stat.stack.macro`），引擎自己的 `[return]` 只做
  `popStack("call")` + `nextOrderWithIndex(caller)`，**不清理宏帧**；引擎注释直接写
  了限制：“マクロからの場合、ここから、呼び出さないとだめ。**だから macro で
  return は使えない**”。
- 实测轨迹（修复前）：`return` 之后流程回到 **宏的调用点**（场景末尾那条剧情行），
  接着往下演（`newgame.ks:9008…`），`seen_set.ks` 的收尾（清除场景标志 → 跳回图库）
  根本没执行。

**修法**（兼容层，通用，无游戏专属数据）：

- 包 `kag.pushStack`：推入 `call` 条目时把**当前宏栈深度**存进该条目；
- 包 `[return]` 标签：交给引擎前先把 `stat.stack.macro` **截断回该深度**（丢掉被调
  用方开的所有宏帧，保留调用方自己的帧），随后引擎的返回就会落到正确的调用点。
- 这就是 KAG3 内联宏的等价语义；实测修复后轨迹：
  `return(define.ks:2964) → jump seen.ks (seen_set.ks:257)`，宏栈/调用栈归零，
  落在图库首页（已验证该页正常渲染）。
- 回归测试：`tests/test_kag_shims.py::TestMacroReturnCompat`（垫片必须同时包装
  `pushStack` 与 `[return]`，且截断发生在委托给引擎之前）。

**调试手法**：这类“静默跳到别处”的问题靠**包装引擎派发点**定位：
`kag.ftag.master_tag[name].start`（而不是 `ftag.startTag`）才是顺序流的真正执行点；
再包 `kag.endStorage` 就能看到回栈与后续跳转。

## 4. `[iscript]` 里的函数声明不会成为全局函数（静默哑掉一整类功能）

**症状**：BGM、音效都正常，但**人物对话语音完全没有**（既没声音、也没有网络请求），
语音重播按钮、自动模式、声音测试页也一起失效。控制台**无任何报错**。

**机制**（已实测定位）：

- KAG3 的 `[iscript]` 代码在**全局作用域**执行，所以 `function CharVoiceFlgCheck(){}`
  是全局函数；Tyrano 用 `evalScript()` 里的 `eval(str)` 执行同一段代码，
  **函数声明只会落在调用方的函数作用域**，不会挂到全局。
- 于是游戏自己的实现不可见，调用点（`[if exp="CharVoiceFlgCheck(mp.vo)==1"]`）
  静默落到**运行时垫片的兜底桩**（`window.X = window.X || function(){return 0;}`）→
  判定永远为假 → 整块语音播放被跳过。与 `[eval]`/`[embScript]` 不同，
  这种失配既不报错也不警告。（`var x = ...` 同样需要导出，转换器早就在做。）
- **判据**：在页面里比对「函数是否存在」与「返回值」——
  `typeof window.Foo` 是 function 但 `Foo(真实参数)` 返回兜底值（且不带参数求值）就是
  桩函数在接管；同时看服务端请求日志：修复前 `GET /data/sound/v_*.ogg` **为 0 次**，
  修复后立刻出现 200。
- **修法**：转换器在生成 `.ks` 时把 iscript 顶层名字导出为全局
  （`kirikiri/kag/scenario.py::_export_globals`）。注意声明的两种花括号风格都要认，
  `function f(a) {` 与 `function f(a)` 换行 `{` **在本类语料里各占一半**；
  旧实现只认同行花括号，导致 16 个顶层函数里有 11 个没被导出。
- **回归测试**：`tests/test_convert_kag.py::TestExportGlobals`（同行/换行花括号、
  嵌套函数不导出、匿名函数表达式不导出）。
- **连带恢复**：本次修好的是语音、语音重播、自动模式、声音测试页与一个音量拆分
  辅助函数——凡是「由游戏 iscript 定义、又被 `[exp]/[if]/[hact]` 调用」的函数都属同一类。

## 5. 命令接线回归：库函数全绿，`audio` 命令却把引用全改悬空

**症状**：68 个 mp3 全部转成了 ogg（0 失败），但 `data/scenario/*.ks`
里**没有一条引用被改写成 `.ogg`**——所有 mp3 文件已被删除，于是每条
`storage=`/`clickse=` 都是悬空引用：**游戏全程无声**，而 `verify` 当时
的布局 / 存档后端 / 音频引用 / PNG 上限检查**全部 OK**。

**根因不在库，而在命令接线**：`tyrano/audio.py` 有两个入口——

- `convert()`：**先** `rewrite_script_refs()` 再 `convert_all()`（正确；
  转码会删掉 mp3，顺序反了就把每条引用变悬空）；
- `convert_all()`：只转码。

CLI 迁移到 Typer 时（argparse → `rpgmaker/cli.py`），`audio` 命令被接成了
`convert_all()`，引用重写这一步静默丢失。**已有单测全部直接调
`tyrano/audio.py` 的函数**，没有一条走命令入口，所以整类回归无人看守。

**恢复**：`_resolvable()` 本身就设计了「源 mp3 已不在、但同名 `.ogg` 已在」
的分支（幂等重跑），所以只需**把命令改回 `convert()` 再跑一次**——它把
63 处引用全部改写、0 悬空，且 0 个文件需要重新转码。刻意保留了那个分支
是有价值的，别删。

**教训**：

- 一个能力只有一个入口后，**命令接线本身就是回归面**：只测库函数会漏掉
  “命令少调了一步”这种最致命的错。现在 `tests/test_tyrano_pipeline.py`
  的 `TestAudioCommandWiring` 直接调 `cli.cmd_tyrano_audio(...)` 并断言
  `.ks` 引用被改写（验证过修复前会红）。新增命令/改接线时照此加一条。
- **交付前要看到“声”才算完**：verify 的静态检查不可能发现「引用悬空」
  这类成对关系（它只看构建里还剩什么）。跑 HTTP 试玩时真的点进去
  听一句；或直接数引用扩展名分布。
- `--sample N` 会先重写**全部**引用、只转前 N 个文件 ⇒ 在真实构建上是
  **半改状态**。要按需采样测速，就在临时目录里单独跑几个文件。

## 6. 从日文原版重写译文会静默还原音频引用（重跑译文/改标点时的陷阱）

**症状**：只改了译文的标点（把一串中点改成更长的中文省略号）后重新写回，
`verify` 突然报 **61 处 `.mp3` 悬空引用** —— 而这个构建前一天还是好的。

**根因**：标点改写要重跑「字典 → `.ks`」的写回，而写回必须从**日文原版**
的场景树出发才对齐（拿已译构建当输入时，键是日文、文件已是中文，命中数
会静默崩成一百多行）。但音频引用（`.mp3` → `.ogg`）是 `audio` 步骤在
**构建里**改的，原版里永远还是 `.mp3` —— 从原版重写译文就把引用一并
还原了。

**恢复**：`audio` 步骤本来就是幂等的（`_resolvable()` 有「源 mp3 已不在、
同名 `.ogg` 已在」的分支）：重跑一次即可 `61 refs rewritten, 0 dangling,
0 converted`。**不要**手工改 `.ks` 引用。

**教训**：

- 「只改译文的某类字符」不是纯文本操作：它重跑了写回链，而写回链的输入
  是原版场景树。重写后必须重跑 **引用审计**（按扩展名数一遍
  `storage=`/`clickse=` 等引用）与 `verify --source`，别只看译文。
- 顺序上也别弄反：`apply_tyrano_translation --scenario-dir <已译构建>` 能跑但
  结果无意义（键无命中、大部分行原样拷回）；正确输入是
  `<原版解包>/data/scenario`。命中数就是判据：满额（本例 540）对，
  骤降（119）就是拿错了输入。
- 同一类“静默还原”还可能发生在：改了字体策略后重跑 `build`（会 rmtree 工作
  目录，转码过的音频全没）、把已处理构建当输入重跑任何转换步骤。重跑前
  先问“这一步的输入是原版还是构建”。

## 7. 省略号分级改写（`・・・・・` → 中文省略号）

owner 规则：原文的 `・` 串按长度分级改写成中文省略号（`・` 在译文里是禁项）：

| 原文点数 | 省略号字数 |
|---|---|
| 2-3 | 2（`……`） |
| 4-5 | 4（`…………`） |
| 6-8 | 6（`………………`） |
| 9+ | 8（封顶） |

机械做法：逐行把**日文键里的第 i 个 `・` 串**对应到**译文里的第 i 个 `…` 串**，
只改长度、不动其它字符。两点必须注意：

- **行内串数可能不等**：译者会合并（`イくっ・・・イぐぅ・・・` 只留一个
  `……`）或拆分停顿（一句里插了三个省略号），也可能原文用的是另一个
  字符（`‥‥` 是 U+2025 双点前导符，不是 `・`）。这些行整行不动或
  **从左按序配对**后报告出来人工过目，**不能猜**。
- 改写后要重跑假名/换行门禁与构建级扫描（省略号不在假名类里，但换行
  数、标签、行数门禁照旧适用），并**目视确认没有溢出消息窗口**——
  变长的是显示文本。

## 8. Electron 运行时漏剥：`preload.js`

剥离清单（`tyrano/build.py::ELECTRON_RUNTIME`）曾漏掉 `preload.js`。它是
Electron 的主进程↔渲染进程桥（`require("electron")`、`contextBridge`），
在 HTML5 构建里是个**永远不会被加载**的死文件（`index.html` 不引用它），
但会连带一堆 Node-only 依赖名留在交付包里。判定方法：构建根应当只剩
`index.html` + `data/` + `tyrano/`；多出来的根文件逐个查是否 Electron/
Node 专属（`preload.js`、`main.js`、`package.json`、`node_modules/`）。
注意 **`app.asar` 里也可能带 `preload.js`**（它会打进包里），所以剥离
要同时覆盖解包后的构建根。

## 9. 引擎 UI 在 scenario 之外：`tyrano/lang.js`

`tyrano/lang.js` 装的是**引擎自己的玩家可见文案**（回标题确认框、无存档
提示、脚本报错、补丁提示……），它在 `data/scenario` **之外**，任何
scenario 提取器都看不到。后果是一类极具欺骗性的“半翻”交付：剧情全中文，
一按“回标题”弹出日文确认框。

- 判定手法：把构建里所有含假名的文件按“非注释行”过一遗（注释里的日文
  不算），逐个问“这行会不会被画出来”。`tyrano/html/*.html`（仅注释）、
  `data/system/KeyConfig.js`（仅注释）、插件 `*.builder.js`（仅注释/console）
  都属安全；`lang.js` 的 `word` 对象才是真文本。
- 处理：`tyrano/pipeline.py localize-ui --dump/--map`（模块
  `tyrano/ui_lang.py`），仓库自带可复用映射 `tyrano/ui_lang_zh.json`。
- **按键匹配，不按原文匹配**：原文随引擎版本微调，键名稳定。
- **机械门禁逐条拒**：占位符集合（`{ name }` 等）、`\n` 个数、残留假名、
  空值——不合格的条目保持原字节并让命令非零退出（翻译质量靠执行者，
  但“会把引擎消息改坏”的错不允许上盘）。
- 单引号风格要跟随原文：stock 文件只在句子含 `"` 的条目上用单引号；写回时
  选不需转义的引号字符。

## 10. `iscript` 里的可见字符串提取器不收

游戏自己的 `[iscript]` 代码里可能嵌着**真要给玩家看的字符串**（典型：
配置画面的文字速度样张 `tf.text_sample = '…'`）。提取器有意不碰
`[iscript]`（那是代码，里面绝大多数是逻辑），于是它既不在 `ja.txt` 里，
也不会被任何门禁抓到。

- 交付前用 `rg` 找 `[iscript]` / `[endscript]` 之间带引号的日文，逐个
  确认是否显示；要译就单独请执行者给一行译文，再整行替换（保留
  `变量 = '…';` 的代码骨架）。
- 同类还有 `tf.*` 赋值、`TG.config.* = "true"` 之类的配置行——它们带
  的**行尾注释**日文永远不用译（不显示）。区分判据就是“会不会被画出来”。

## 11. 音频标签的四个引擎事实（钩子测轨迹时必须知道）

移植 KAG3 音频时踩到的、**不看引擎源码就会误判**的四条：

1. **`[playse]` 内部就是调 `[playbgm]`。** `kag.tag_audio.js` 里
   `tyrano.plugin.kag.tag.playse.start` 直接 `this.kag.ftag.startTag("playbgm", pm)`，
   `pm.target="se"` 才区分。所以给 `master_tag['playbgm'].start` 挂钩子会**同时**
   收到 SE/语音事件；`[stopse]` 同理进 `stopbgm`。要按目标分流必须看 `pm.target`，
   否则会把语音当成 BGM 改动去查（我因此白查了一轮）。
2. **影片层默认静音。** `kag.tag.js` 的 `layermode_movie`：
   `if (pm.volume != "") video.volume = 小数; else video.volume = 0;` ——
   不传 `volume` 的影片**一定无声**。KAG3 的 `[playvideo]` 不含该参数，
   所以垫片必须补默认值（现为 `100`）。症状是“影片在放但只有 BGM”，极易
   被误诊成“BGM 没停”。
3. **同一 storage 的 `[playbgm]` 走跳过路径。** 引擎记住
   `stat.current_bgm`（值是解析后的路径，如 `../bgm/x.ogg`），同名时不再重放。
   对“游戏用同一首曲子做回放开场”的写法，效果就是**无缝接着放**，玩家
   听不出“回放开始了”。要在回放模式下强制重启，先清 `stat.current_bgm`
   （比 storage 要用 stem：两侧分别是裸名与完整路径）。
4. **淡出的 BGM 会被引擎“丢失”。** `stopbgm`（`[fadeoutbgm]` 只是
   `pm.fadeout="true"` 转调它）在淡出分支里**先** `delete
   target_map[key]`、**再** `audio_obj.fade(...)`；而 `playbgm` 停旧曲靠的正是
   那张 `kag.tmp.map_bgm[buf]` 表。后果：淡出中的曲子已不在表里，下一个
   `[playbgm]` 停不掉它，两首叠着放满整个 `time`（本作 3 s）。要按 KAG3
   单槽语义挖掉它，必须在 `stopbgm` 的**前置**钩子里把即将被淡出的 Howl
   **对象**存下来（只看 `target=bgm`、`buf_all` 要取整张表），再在新
   `[playbgm]`（非 `se`）里 `stop()+unload()` —— 存对象而非曲名，才不会
   误伤 SE/语音；没有后续 BGM 时淡出照常跑完。
5. **独立源垫片不可单独做语法检查。** `kirikiri/kag/js/*.js` 是**片段**
   （如 `noop_plus_real.js` 顶层没有外层 IIFE，直接当文件解析必然报
   `parse error`——而且 **HEAD 版本也报**），真正的语法门禁是**组装产物**
   `tyrano/plugins/kag/kag.tag_kag3shim.js`。别把片段文件的解析失败
   当成自己改错了（血泪：白查了一轮）。
6. **换屏后第一条文本被吃掉：一次手势只能满足一个等待。** KAG3 `[jump]` 是
   同步的 `process()`，点击事件还没走完，新场景已把点击层 / `[p]` 配好，于是
   同一次点击被新画面又消费一遍（实观：进鉴赏自动进第一个 CG、进回想第一句
   被跳过）。名字就写着语义：`nextOrder()` = 让当前 order 前进一格，**不是**
   每个输入事件推进一次；被违反的不变量是「一次物理手势最多满足一个等待」。
   正确做法是**状态边界**而非时长：跳转时升起输入屏障，新屏完成一次绘制
   （`requestAnimationFrame` + 一个任务边界）后落下；屏障期间在捕获阶段
   （关键事件再加冒泡）吞掉**该手势的整个尾巴**——mouseup / click / touchend
   / pointerup / keyup 一律覆盖，因此**不必枚举事件类型**，也就没有毫秒常量。
   `keydown` 有意排除：它负责开系统菜单与选选项，不推进文本。
   反面证据：初版猜了个 300 ms 冷却且只覆盖 `mousedown`+`click`，于是触摸路径
   （WebView/JoiPlay 的主路径）与初始化慢的屏漏了出去，还会吞掉玩家正常快速
   点击。另：**推进的调用点不可枚举**（十余个模块、数百处，且含引擎内核自带
   的 handler），所以屏障要么放在唯一收口，要么放在输入层——不要逐路径补。
   顺带清掉继承来的 weak-stop 状态（`cancelWeakStop()` / `is_click_text`）。
7. **`[rclick]` 的“系统菜单默认”原本是显式 no-op，现已补上浮层菜单。**
   原注释：Plain `[rclick enabled=true]`（无 jump）是系统菜单默认、注册成
   null —— “the port has no system menu overlay”，于是剧情里**根本没有
   菜单**、回不到主页菜单。现在：右键打开垫片自己的浮层（条目由
   `--title-jump` 注入，目标不写死），面板行里也有 `MENU` 入口；
   「回到主页菜单」= 清栈后 process() 跳转。

